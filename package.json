{
  "name": "rapidflow-plumbing",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "type-check": "tsc --noEmit",
    "test-sms": "cross-env NODE_OPTIONS=--openssl-legacy-provider ts-node --project tsconfig.scripts.json scripts/test-sms.ts",
    "test-inbound": "cross-env NODE_OPTIONS=--openssl-legacy-provider ts-node --project tsconfig.scripts.json scripts/test-inbound-endpoint.ts",
    "test-whatsapp": "cross-env NODE_OPTIONS=--openssl-legacy-provider ts-node --project tsconfig.scripts.json scripts/test-whatsapp-webhook.ts",
    "test-reset": "cross-env NODE_OPTIONS=--openssl-legacy-provider ts-node --project tsconfig.scripts.json scripts/test-reset-endpoint.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.55.0",
    "@upstash/redis": "^1.38.0",
    "googleapis": "^144.0.0",
    "next": "^16.2.4",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "twilio": "^5.4.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "cross-env": "^10.1.0",
    "ts-node": "^10.9.2",
    "typescript": "^5"
  }
}
